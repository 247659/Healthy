server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: patient-service-route
              uri: http://localhost:8081
              predicates:
                - Path=/api/v1/patients/**
            - id: medical-staff-service-route
              uri: http://localhost:8082
              predicates:
                - Path=/api/v1/medical-staff/**